# ImageResultCard

This is an example component.

```jsx
import { ImageResultCard } from 'ai-jimu';

export default () => {
  const props = {
    loading: true,
  };
  return <ImageResultCard {...props} />;
};
```
